# Web-App-SE-lab3
Servlet that records every request made to the servlet (time of
access, host, path).
Store a log in a static variable on the servlet class (use an
ArrayList) for example (see the documentation for
HttpServletRequest).
Deploy your application either locally and access from your
browser at localhost:8080
